#include <stdio.h>
#include <math.h>
    int main()
{
    float a,c,b;
    printf("c=");
    scanf("%g",&c);
    if (a=2) b=(a*a-4*c)/(a*c);
    else if (a=4) b=sqrt (a*c-2*a);
    else (a=6);b=(c*c-2*a);
    printf("b=%g", b); 
    return 0;

}
